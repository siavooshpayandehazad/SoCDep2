__author__ = 'siavoosh'


def Add_TG_EdgeTo_Router (TG, AG, Edge, Link, batch, Prob, StartTime, EndTime, logging):

    return True

def FindLastAllocatedTimeOnRouter(TG, AG, Link, logging=None):
    LastAllocatedTime = None
    return LastAllocatedTime


def FindLastAllocatedTimeOnRouterForTask(TG, AG, Link, Edge, Prob, logging):
    logging.info("\t-------------------------")
    logging.info("\tFINDING LAST ALLOCATED TIME ON Router "+str(Link)+"\tFOR EDGE: "+str(Edge)+" WITH PROB: "+str(Prob))
    LastAllocatedTime = 0
    return LastAllocatedTime